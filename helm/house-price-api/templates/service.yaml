apiVersion: v1
kind: Service
metadata:
  name: house-price-api-service
spec:
  selector:
    app: house-price-api
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: ClusterIP